# Keep REST calls on the same Windows-visible origin and let Nginx route them.
VITE_TRADE_LIBRARY_URL=/trade-library
VITE_PRICING_URL=/pricing
VITE_RISK_URL=/risk

# Optional. Omit this value to derive ws:// or wss:// from the page URL.
# VITE_STREAM_URL=ws://192.168.200.128:8080/stream
